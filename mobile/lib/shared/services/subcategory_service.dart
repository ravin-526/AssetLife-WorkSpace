import '../../core/api/api_client.dart';

class SubcategoryService {
  final ApiClient apiClient;

  SubcategoryService(this.apiClient);

  // Not needed for now, as subcategories come with categories in /api/categories
}
